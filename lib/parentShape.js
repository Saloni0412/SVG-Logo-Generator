class parentShape {
    constructor(text, shapeColor, textColor) {
        this.text = text;
        this.shapeColor = shapeColor;
        this.textcolor = textColor;
    }
}

module.exports = parentShape;